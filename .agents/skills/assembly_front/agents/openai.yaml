interface:
  display_name: "Assembly Front"
  short_description: "Repo-specific patterns and workflows for assembly_front"
  default_prompt: "Use the assembly_front repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true